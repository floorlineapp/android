<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Session tokens must never leave the device via backup. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore/" />
</full-backup-content>
